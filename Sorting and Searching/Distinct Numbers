#include<bits/stdc++.h>
using namespace std;
map<int,bool> mp;
 
int main(){
  ios::sync_with_stdio(0); cin.tie(0);
  int n,ans=0;
  cin >> n;
  for(int i=1;i<=n;i++){
    int m;
    cin >> m;
    if(mp[m]==false){
      mp[m]=true;
      ans++;
    }
  }
  cout << ans;
}
